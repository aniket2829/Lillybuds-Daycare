import { ADLaM_Display, Comfortaa } from "next/font/google";

export const adlamDisplay = ADLaM_Display({
  subsets: ["latin"],
  weight: "400",
});

export const comfortaa = Comfortaa({ subsets: ["latin"] });
